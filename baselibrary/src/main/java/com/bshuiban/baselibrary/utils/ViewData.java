package com.bshuiban.baselibrary.utils;

/**
 * Created by xinheng on 2018/1/25.<br/>
 * describe：view 基本数据
 */

public class ViewData {
    /**边框弧度*/
    public static final int radius=10;
    /*private void drawBackGroundBord(Canvas canvas,int color,int witdh,int height) {
        Paint paint = new Paint(Paint.ANTI_ALIAS_FLAG);
        paint.setAntiAlias(true);
        paint.setColor(Color.WHITE);
        paint.setStyle(Paint.Style.FILL);
        RectF rectF = new RectF(0, 0, witdh, height);
        canvas.drawRoundRect(rectF, ViewData.radius, ViewData.radius, paint);
        paint.setColor(color);
        paint.setStyle(Paint.Style.STROKE);
        canvas.drawRoundRect(rectF, ViewData.radius, ViewData.radius, paint);
    }*/
}
